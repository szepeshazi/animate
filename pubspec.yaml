name: animate
description: Flutter animation showcase
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.1ß

environment:
  sdk: '>=3.1.0'

dependencies:
  flutter:
    sdk: flutter
  dashbook:
  flutter_launcher_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  shaders:
    - assets/shaders/exp.frag

flutter_icons:
  android: "animate_icon"
  ios: "animate_icon"
  image_path: "assets/icon/metaball.png"
